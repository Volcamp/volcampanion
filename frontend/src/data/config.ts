/** Single place to bump when rolling the app to a new edition. */
export const DATA_URL = `${import.meta.env.BASE_URL}data/volcamp-2026.json`

/** localStorage key holding the array of favorited talk ids. */
export const FAVORITES_KEY = 'favorites'

/** Cache Storage bucket used as the offline fallback for the data bundle. */
export const DATA_CACHE = 'volcamp-data'
